497  init 0
  498  docker network create --driver bridge --subnet 172.16.0.0/24 reseau-interne-serveur
  499  cd configuration-dns/
  500  docker build -t soa-externe .
  501  docker exec -ti soa-externe /bin/bash
  502  docker run -d --network reseau-interne-serveur --ip 172.16.0.53 --name soa-externe
  503* docker run -d --network  --ip 172.16.0.53 --name soa-externe soa-externe
  504  docker ps
  505  docker ps -l
  506  docker run -d --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe
  507  docker run -d --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  508  docker container rm soa-externe
  509  docker run -d --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  510  docker ps
  511  docker ps -l
  512  docker start soa-externe 
  513  docker ps -l
  514  docker container rm soa-externe
  515  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  516  docker ps -l
  517  docker container ls -l
  518  docker container rm soa-externe
  519  docker run -tid --network reseau-interne-serveur  --ip 172.16.0.53 soa-externe
  520  docker ps -l
  521  docker container rm soa-externe
  522  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name dns-public soa-externe
  523  docker ps -l
  524  docker container rm dns-public 
  525  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 soa-externe
  526  docker ps -l
  527  docker container rm nostalgic_
  528  docker container rm nostalgic_nash 
  529  docker images ls -l
  530  docker images ls 
  531  docker images ps
  532  docker images 
  533  docker images rm serveur-dns
  534  docker images rm -f serveur-dns
  535  docker images 
  536  docker images --help
  537  docker images -a
  538  docker logs soa-externe
  539  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  540  docker logs soa-externe
  541  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  542  docker container rm soa-externe 
  543  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  544  docker ps
  545  docker ps -l
  546  docker logs soa-externe
  547  docker build -t soa-externe .
  548  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  549  docker container rm soa-externe 
  550  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  551  docker ps -l
  552  docker logs soa-externe
  553  docker container rm soa-externe 
  554  docker build -t soa-externe .
  555  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  556  docker ps -l
  557  docker exec -it soa-externe 
  558  docker exec -it soa-externe bash
  559  docker container rm soa-externe 
  560  docker container stop soa-externe 
  561  docker stop soa-externe
  562  docker ps
  563  docker container rm soa-externe 
  564  docker build -t soa-externe .
  565  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  566  docker ps
  567  docker exec -it soa-externe bash
  568  docker stop soa-externe
  569  docker container rm soa-externe 
  570  docker build -t soa-externe .
  571  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  572  docker exec -it soa-externe bash
  573  docker container rm soa-externe 
  574  docker stop soa-externe
  575  docker container rm soa-externe 
  576  docker build -t soa-externe .
  577  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  578  docker exec -it soa-externe bash
  579  docker stop soa-externe
  580  docker container rm soa-externe 
  581  docker stop soa-externe
  582  docker container rm soa-externe 
  583  docker build -t soa-externe .
  584  docker run -tid --network reseau-interne-serveur --network-alias soa-externe --ip 172.16.0.53 --name soa-externe soa-externe
  585  docker exec -it soa-externe bash
  586  docker stop soa-externe
  587  history
